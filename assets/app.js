const SUPPORTED=['en','zh-CN'];
const state={lang:'en',countries:[],rankings:new Map(),confidence:new Map(),manifest:{},dict:{}};
function detectLanguage(){const saved=localStorage.getItem('kingai-lang');if(SUPPORTED.includes(saved))return saved;const langs=navigator.languages?.length?navigator.languages:[navigator.language||'en'];return langs.some(v=>/^zh\b/i.test(v))?'zh-CN':'en'}
async function loadJSON(path,fallback){try{const res=await fetch(path,{cache:'no-store'});if(!res.ok)throw new Error(`${res.status} ${path}`);return await res.json()}catch(err){console.warn(err);return fallback}}
function locale(){return state.lang==='zh-CN'?'zh-CN':'en-US'}
function localizedCountryName(country){try{const name=new Intl.DisplayNames([locale()],{type:'region'}).of(country.cca2);if(name&&name!==country.cca2)return name}catch{}return state.lang==='zh-CN'?(country.name_zh||country.name_en):country.name_en}
function localizedRegion(region){const zh={Africa:'非洲',Americas:'美洲',Asia:'亚洲',Europe:'欧洲',Oceania:'大洋洲',Antarctic:'南极洲'};return state.lang==='zh-CN'?(zh[region]||region||'—'):(region||'—')}
function scoreCell(value,invert=false){if(value===null||value===undefined||Number.isNaN(Number(value)))return'<span class="muted">—</span>';const n=Math.max(0,Math.min(100,Number(value)));const title=invert?`${n}/100 risk`:`${n}/100`;return `<span title="${title}">${n.toFixed(0)}</span>`}
function confidenceLabel(v){const map={'very-high':state.dict.evidence_confidence_very_high||'Very high','high':state.dict.evidence_confidence_high||'High','moderate':state.dict.evidence_confidence_moderate||'Moderate','low':state.dict.evidence_confidence_low||'Low','very-low':state.dict.evidence_confidence_very_low||'Very low'};return map[v]||'—'}
function confidenceCell(row){if(!row||!Number.isFinite(Number(row.evidence_coverage_score)))return'<span class="muted">—</span>';const n=Math.max(0,Math.min(100,Number(row.evidence_coverage_score)));return `<span title="${escapeHTML(confidenceLabel(row.evidence_coverage_label))}">${n.toFixed(0)} · ${escapeHTML(confidenceLabel(row.evidence_coverage_label))}</span>`}

/* KINGAI Warm Life · Apple-style one-line marketing typography */
const FLUID_SELECTOR='[data-fit-line],.hero h1,.home-modules h2,.methodology h2';
function fluidLimits(el){return el.matches('.hero h1')?[30,80]:[24,60]}
function fitFluid(el){const[min,max]=fluidLimits(el),width=el.clientWidth||el.parentElement?.clientWidth||0;if(!width)return;el.style.maxWidth='100%';el.style.whiteSpace='nowrap';el.style.textWrap='nowrap';let lo=min,hi=max;for(let i=0;i<10;i++){const mid=(lo+hi)/2;el.style.fontSize=`${mid}px`;if(el.scrollWidth<=width+.5)lo=mid;else hi=mid}el.style.fontSize=`${Math.floor(lo*10)/10}px`;if(el.scrollWidth>width+1&&lo<=min+.6){el.style.whiteSpace='normal';el.style.textWrap='balance'}}
let fluidFrame=0;
function runFluidType(){cancelAnimationFrame(fluidFrame);fluidFrame=requestAnimationFrame(()=>document.querySelectorAll(FLUID_SELECTOR).forEach(fitFluid))}
function initFluidType(){runFluidType();window.addEventListener('resize',runFluidType,{passive:true});window.addEventListener('orientationchange',runFluidType,{passive:true});document.fonts?.ready?.then(runFluidType);if('ResizeObserver'in window){const ro=new ResizeObserver(runFluidType);document.querySelectorAll(FLUID_SELECTOR).forEach(el=>ro.observe(el))}}

async function applyTranslations(){const[base,media]=await Promise.all([loadJSON(`/i18n/${state.lang}.json`,{}),loadJSON(`/i18n/media-${state.lang}.json`,{})]);state.dict={...base,...media};document.documentElement.lang=state.lang;document.querySelectorAll('[data-i18n]').forEach(el=>{const key=el.dataset.i18n;if(state.dict[key])el.textContent=state.dict[key]});document.querySelectorAll('[data-i18n-placeholder]').forEach(el=>{const key=el.dataset.i18nPlaceholder;if(state.dict[key])el.placeholder=state.dict[key]});document.querySelectorAll('[data-lang]').forEach(btn=>btn.classList.toggle('active',btn.dataset.lang===state.lang));document.title=state.dict.page_title||'KINGAI Global Intelligence';runFluidType()}
function renderStatus(){document.querySelector('#coverage-count').textContent=state.countries.length?state.countries.length.toLocaleString(locale()):'—';document.querySelector('#last-update').textContent=state.manifest.generated_at?new Intl.DateTimeFormat(locale(),{dateStyle:'medium',timeStyle:'short'}).format(new Date(state.manifest.generated_at)):'—';document.querySelector('#methodology-version').textContent=state.manifest.methodology_version||'—'}
function renderTable(){const q=document.querySelector('#country-search').value.trim().toLocaleLowerCase(locale());const rows=state.countries.map(c=>({...c,localizedName:localizedCountryName(c)})).filter(c=>!q||c.localizedName.toLocaleLowerCase(locale()).includes(q)||c.name_en.toLowerCase().includes(q)||c.cca2.toLowerCase().includes(q)||c.cca3.toLowerCase().includes(q)).sort((a,b)=>a.localizedName.localeCompare(b.localizedName,locale())).map(c=>{const s=state.rankings.get(c.cca3)||{};const conf=state.confidence.get(c.cca3)||{};return `<tr><td><a class="country-name" href="/country.html?c=${encodeURIComponent(c.cca3)}"><span class="flag">${c.flag||''}</span>${escapeHTML(c.localizedName)}</a></td><td>${escapeHTML(localizedRegion(c.region))}</td><td>${scoreCell(s.safety)}</td><td>${scoreCell(s.wellbeing)}</td><td>${scoreCell(s.human_rights)}</td><td>${scoreCell(s.expression)}</td><td>${scoreCell(s.governance)}</td><td>${scoreCell(s.live_risk,true)}</td><td>${confidenceCell(conf)}</td></tr>`}).join('');const tbody=document.querySelector('#country-table-body');tbody.innerHTML=rows||`<tr><td colspan="9" class="loading">${state.lang==='zh-CN'?'没有匹配的国家或地区。':'No matching countries or territories.'}</td></tr>`}
function escapeHTML(value=''){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
async function setLanguage(lang){state.lang=SUPPORTED.includes(lang)?lang:'en';localStorage.setItem('kingai-lang',state.lang);await applyTranslations();renderStatus();renderTable()}
async function boot(){state.lang=detectLanguage();const[manifest,countriesDoc,rankingsDoc,confidenceDoc]=await Promise.all([loadJSON('/data/manifest.json',{}),loadJSON('/data/countries.json',{countries:[]}),loadJSON('/data/rankings.json',{countries:[]}),loadJSON('/data/confidence.json',{countries:[]})]);state.manifest=manifest;state.countries=Array.isArray(countriesDoc)?countriesDoc:(countriesDoc.countries||[]);const rankingRows=Array.isArray(rankingsDoc)?rankingsDoc:(rankingsDoc.countries||[]);state.rankings=new Map(rankingRows.map(r=>[r.cca3,r]));state.confidence=new Map((confidenceDoc.countries||[]).map(r=>[r.cca3,r]));await applyTranslations();renderStatus();renderTable();document.querySelector('#country-search').addEventListener('input',renderTable);document.querySelectorAll('[data-lang]').forEach(btn=>btn.addEventListener('click',()=>setLanguage(btn.dataset.lang)));initFluidType()}
boot();
