const SUPPORTED=['en','zh-CN'];
const state={lang:'en',dict:{},latest:{},sources:{},manifest:{},globalNews:{items:[],domains:[]},clusters:{clusters:[]},domainIndex:{discovered_domains:[]},health:{overall:'inactive',summary:{}},coverage:{summary:{},countries:[],languages:[]},trends:{summary:{},topic_trends:[],diversity:{},narrative_by_language:[]},filter:'all'};
function detectLanguage(){const saved=localStorage.getItem('kingai-lang');if(SUPPORTED.includes(saved))return saved;const langs=navigator.languages?.length?navigator.languages:[navigator.language||'en'];return langs.some(v=>/^zh\b/i.test(v))?'zh-CN':'en'}
async function loadJSON(url,fallback){try{const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`${r.status} ${url}`);return await r.json()}catch(e){console.warn(e);return fallback}}
function t(k,f=k){return state.dict[k]||f}
function escapeHTML(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function safeURL(v){try{const u=new URL(v);return /^https?:$/.test(u.protocol)?u.href:null}catch{return null}}
function formatDate(v){if(!v)return'—';const d=new Date(v);if(Number.isNaN(d.getTime()))return String(v);return new Intl.DateTimeFormat(state.lang==='zh-CN'?'zh-CN':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(d)}
function number(v){return Number.isFinite(Number(v))?new Intl.NumberFormat(state.lang==='zh-CN'?'zh-CN':'en-US').format(Number(v)):'—'}
async function applyTranslations(){const[base,page]=await Promise.all([loadJSON(`/i18n/${state.lang}.json`,{}),loadJSON(`/i18n/media-${state.lang}.json`,{})]);state.dict={...base,...page};document.documentElement.lang=state.lang;document.querySelectorAll('[data-i18n]').forEach(el=>{const k=el.dataset.i18n;if(state.dict[k])el.textContent=state.dict[k]});document.querySelectorAll('[data-i18n-placeholder]').forEach(el=>{const k=el.dataset.i18nPlaceholder;if(state.dict[k])el.placeholder=state.dict[k]});document.querySelectorAll('[data-lang]').forEach(btn=>btn.classList.toggle('active',btn.dataset.lang===state.lang));document.title=state.lang==='zh-CN'?'全球媒体情报 — KINGAI':'Global Media Intelligence — KINGAI'}
function allItems(){const l=state.latest||{};const source=[...(l.publisher_feeds||[]),...(state.globalNews?.items||[]),...(l.open_web||[]),...(l.public_social||[]),...(l.event_linked_sources||[]),...(l.videos||[])];const seen=new Set();return source.filter(item=>{const key=item.url||`${item.media_kind}:${item.title||item.text||item.event_id||''}`;if(seen.has(key))return false;seen.add(key);return true})}
function kindLabel(k){const map={'publisher-feed':t('media_publishers','Publisher feeds'),'open-web':t('open_web','Open web'),'public-social':t('public_social','Public social'),'event-linked-source':t('media_event_links','Event links'),'video':t('media_video','Video')};return map[k]||k||'Media'}
function itemTitle(item){return item.title||item.text||item.event_id||'Untitled'}
function itemPublisher(item){return item.publisher||item.channel_title||item.domain||item.author_handle||item.platform||item.source_id||'—'}
function itemDate(item){return item.published_at||item.created_at||item.collected_at||null}
function renderStream(){const q=document.querySelector('#media-search').value.trim().toLowerCase();let items=allItems().filter(v=>state.filter==='all'||v.media_kind===state.filter);if(q)items=items.filter(v=>`${itemTitle(v)} ${itemPublisher(v)} ${v.platform||''} ${v.domain||''} ${v.cca3||''} ${v.source_country||''} ${v.topic||''} ${v.query_language||''}`.toLowerCase().includes(q));items.sort((a,b)=>new Date(itemDate(b)||0)-new Date(itemDate(a)||0));const html=items.slice(0,240).map(item=>{const url=safeURL(item.url);const title=escapeHTML(itemTitle(item));const pub=escapeHTML(itemPublisher(item));const summary=escapeHTML(item.summary||item.text||item.location||'');const meta=[kindLabel(item.media_kind),pub,item.topic||'',item.query_language||item.language||'',item.cca3||item.publisher_country||item.source_country||'',formatDate(itemDate(item))].filter(Boolean).join(' · ');return `<article class="media-card"><div class="media-card-meta"><span class="evidence-badge unverified">${escapeHTML(kindLabel(item.media_kind))}</span><small>${escapeHTML(meta)}</small></div><h3>${url?`<a href="${escapeHTML(url)}" target="_blank" rel="noopener noreferrer">${title}</a>`:title}</h3>${summary?`<p>${summary}</p>`:''}<div class="media-card-foot"><span>${escapeHTML(pub)}</span><span>${escapeHTML(t('media_weight_zero','Direct score weight: 0'))}</span></div></article>`}).join('');document.querySelector('#media-grid').innerHTML=html||`<div class="empty-state">${escapeHTML(t('media_no_items','No matching media items in this snapshot.'))}</div>`}
function corroborationLabel(value){if(value==='broadly-corroborated')return t('media_broadly_corroborated','Broadly corroborated');if(value==='corroborated')return t('media_corroborated','Corroborated');return t('media_single_source','Single source')}
function renderClusters(){const rows=(state.clusters?.clusters||[]).filter(v=>Number(v.independent_domain_count||0)>=2).slice(0,24);document.querySelector('#media-clusters').innerHTML=rows.map(cluster=>{const sources=(cluster.items||[]).slice(0,6).map(item=>{const u=safeURL(item.url);const label=escapeHTML(item.publisher||item.domain||'Source');return u?`<a href="${escapeHTML(u)}" target="_blank" rel="noopener noreferrer">${label}</a>`:label}).join(' · ');const risk=cluster?.syndication?.risk;const riskText=risk&&risk!=='none'?` · ${t('media_syndication_risk','Syndication risk')}: ${risk}`:'';return `<article class="cluster-card ${escapeHTML(cluster.corroboration||'single-source')}"><div class="cluster-head"><span class="cluster-badge">${escapeHTML(corroborationLabel(cluster.corroboration))}</span><small>${escapeHTML(String(cluster.independent_domain_count||0))} ${escapeHTML(t('media_independent_domains','independent domains'))}${escapeHTML(riskText)}</small></div><h3>${escapeHTML(cluster.representative_title||'—')}</h3><p>${escapeHTML(t('media_corroboration_not_truth','Source breadth is not a truth score.'))}</p><div class="cluster-sources">${sources||'—'}</div></article>`}).join('')||`<div class="empty-state">${escapeHTML(t('media_no_clusters','No multi-source clusters in this snapshot yet.'))}</div>`}
function healthLabel(v){if(v==='healthy')return t('media_health_healthy','Healthy');if(v==='partial')return t('media_health_partial','Partial');if(v==='degraded')return t('media_health_degraded','Degraded');return t('media_health_inactive','Inactive')}
function renderStatus(){const c=state.manifest?.coverage||{};document.querySelector('#media-seed-count').textContent=number(c.seed_outlets||state.sources?.seed_outlets?.length||0);document.querySelector('#media-collector-count').textContent=number(c.collectors||state.sources?.collectors?.length||0);const declared=['publisher_feed_items','open_web_items','public_social_items','event_linked_sources','video_items'].reduce((n,k)=>n+Number(c[k]||0),0);document.querySelector('#media-item-count').textContent=number(Math.max(declared,allItems().length));document.querySelector('#media-health-state').textContent=healthLabel(state.health?.overall||'inactive');const cs=state.coverage?.summary||{};document.querySelector('#media-country-gaps').textContent=number(Number(cs.gaps||0)+Number(cs.thin||0));document.querySelector('#media-language-count').textContent=number(cs.languages||state.coverage?.languages?.length||0);document.querySelector('#media-domain-count').textContent=number(state.domainIndex?.discovered_domains?.length||0)}
function trendLabel(v){const map={surging:t('media_trend_surging','Surging'),rising:t('media_trend_rising','Rising'),stable:t('media_trend_stable','Stable'),falling:t('media_trend_falling','Falling'),emerging:t('media_trend_emerging','Emerging'),new:t('media_trend_new','New'),flat:t('media_trend_flat','Flat')};return map[v]||v||'—'}
function concentrationLabel(v){const map={broad:t('media_concentration_broad','Broad'),moderate:t('media_concentration_moderate','Moderate'),high:t('media_concentration_high','High'),['insufficient-data']:t('media_concentration_insufficient','Insufficient data')};return map[v]||v||'—'}
function renderTrends(){const d=state.trends?.diversity||{};document.querySelector('#trend-items-24h').textContent=number(state.trends?.summary?.items_24h||0);document.querySelector('#trend-domains-24h').textContent=number(d.independent_domains_24h||0);document.querySelector('#trend-source-countries').textContent=number(d.source_countries_24h||0);document.querySelector('#trend-top5-share').textContent=Number.isFinite(Number(d.top_5_domain_share_pct))?`${Number(d.top_5_domain_share_pct).toFixed(1)}%`:'—';document.querySelector('#trend-concentration-label').textContent=concentrationLabel(d.concentration_label);
  const rows=(state.trends?.topic_trends||[]).slice(0,20).map(v=>`<tr><td>${escapeHTML(v.topic)}</td><td>${number(v.last_6h)}</td><td>${number(v.previous_6h)}</td><td><span class="trend-state ${escapeHTML(v.trend||'stable')}">${escapeHTML(trendLabel(v.trend))}</span></td><td>${number(v.independent_domains_24h)}</td><td>${number(v.languages_24h)}</td></tr>`).join('');document.querySelector('#trend-table').innerHTML=rows||`<tr><td colspan="6" class="loading">${escapeHTML(t('media_no_trends','No trend data in this snapshot yet.'))}</td></tr>`;
  const languages=(state.trends?.narrative_by_language||[]).slice(0,12);document.querySelector('#language-narrative').innerHTML=languages.map(v=>{const topics=(v.top_topics||[]).slice(0,5).map(x=>`${x.topic} ${x.items}`).join(' · ');const domains=(v.top_domains||[]).slice(0,4).map(x=>x.domain).join(' · ');return `<article class="language-card"><div class="language-card-head"><strong>${escapeHTML(v.language)}</strong><span>${number(v.items)} ${escapeHTML(t('media_items','items'))}</span></div><p>${escapeHTML(topics||'—')}</p><small>${escapeHTML(domains||'—')}</small></article>`}).join('')||`<div class="empty-state">${escapeHTML(t('media_no_language_narratives','No language narrative data yet.'))}</div>`}
function renderDirectory(){const rows=(state.sources?.seed_outlets||[]).slice().sort((a,b)=>(a.country||'').localeCompare(b.country||'')||a.name.localeCompare(b.name));document.querySelector('#media-directory').innerHTML=rows.map(v=>`<article class="directory-card"><div><strong>${escapeHTML(v.name)}</strong><small>${escapeHTML(v.country||'—')} · ${escapeHTML((v.languages||[]).join(', ')||'—')}</small></div><div><span>${escapeHTML(v.ownership||'—')}</span><small>${escapeHTML(v.collection||'—')}</small></div></article>`).join('')||`<div class="empty-state">${escapeHTML(t('data_unavailable','Data unavailable'))}</div>`}
function renderDiscovered(){const rows=(state.domainIndex?.discovered_domains||[]).slice(0,180);document.querySelector('#discovered-directory').innerHTML=rows.map(v=>`<article class="directory-card dynamic"><div><strong>${escapeHTML(v.name||v.domain)}</strong><small>${escapeHTML(v.domain)} · ${escapeHTML(v.context_country||(v.source_countries||[]).join(', ')||'—')} · ${escapeHTML((v.languages||[]).join(', ')||'—')}</small></div><div><span>${escapeHTML(v.ownership||`${v.items||0} ${t('media_items','items')}`)}</span><small>${escapeHTML(v.ownership?`${v.items||0} ${t('media_items','items')}`:(v.media_kinds||[]).join(', ')||'—')}</small></div></article>`).join('')||`<div class="empty-state">${escapeHTML(t('media_no_discovered','No automatically discovered domains in this snapshot yet.'))}</div>`}
function renderPlatforms(){const rows=(state.sources?.collectors||[]).map(v=>`<tr><td>${escapeHTML(v.id)}</td><td>${escapeHTML(v.type||'—')}</td><td>${escapeHTML(v.auth||'none')}</td><td><span class="platform-state ${v.enabled?'on':'off'}">${escapeHTML(v.status||(v.enabled?t('media_active','Active'):t('media_inactive','Inactive')))}</span></td></tr>`).join('');document.querySelector('#platform-table').innerHTML=rows||`<tr><td colspan="4" class="loading">${escapeHTML(t('data_unavailable','Data unavailable'))}</td></tr>`}
function render(){renderStatus();renderTrends();renderClusters();renderStream();renderDiscovered();renderDirectory();renderPlatforms()}
async function setLanguage(lang){state.lang=SUPPORTED.includes(lang)?lang:'en';localStorage.setItem('kingai-lang',state.lang);await applyTranslations();render()}
async function boot(){state.lang=detectLanguage();const[latest,sources,manifest,globalNews,clusters,domainIndex,health,coverage,trends]=await Promise.all([loadJSON('/data/media/latest.json',{publisher_feeds:[],open_web:[],public_social:[],event_linked_sources:[],videos:[]}),loadJSON('/data/media/sources.json',{collectors:[],seed_outlets:[]}),loadJSON('/data/media/manifest.json',{coverage:{}}),loadJSON('/data/media/global-news.json',{items:[],domains:[]}),loadJSON('/data/media/clusters.json',{clusters:[]}),loadJSON('/data/media/domain-index.json',{discovered_domains:[]}),loadJSON('/data/media/health.json',{overall:'inactive',summary:{}}),loadJSON('/data/media/coverage.json',{summary:{},countries:[],languages:[]}),loadJSON('/data/media/trends.json',{summary:{},topic_trends:[],diversity:{},narrative_by_language:[]})]);Object.assign(state,{latest,sources,manifest,globalNews,clusters,domainIndex,health,coverage,trends});await applyTranslations();render();document.querySelector('#media-search').addEventListener('input',renderStream);document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{state.filter=btn.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===btn));renderStream()}));document.querySelectorAll('[data-lang]').forEach(btn=>btn.addEventListener('click',()=>setLanguage(btn.dataset.lang)))}
boot();
