{
  "name": "kingai-global-intelligence-site",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name=kingai-global-intelligence",
    "preview:pages": "npm run build && wrangler pages dev dist"
  },
  "devDependencies": {
    "wrangler": "4.124.0"
  }
}
